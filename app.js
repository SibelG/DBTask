const http = require('http');
import express from "express";

const app=express;

app.length("/", (req,res)=>{
    res.send("Index Page");
});
server.listen(3000, () => {
 console.log('Uygulama çalıştırıldı...');
});